# Environment and secrets
.env
.env.local
.env.production
.dev.vars

# macOS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Node / npm (if any tooling gets added)
node_modules/
npm-debug.log*

# Wrangler local state
.wrangler/
wrangler.lock

# Editor
.vscode/
.idea/
*.swp
*.swo

# Build artifacts
dist/

# Client material never belongs in this repo — it is public.
# Session writeups and seed scripts live in the Obsidian vault instead.
# (clients/elevante.md + elevate_seed.sql were removed 2026-08-02; see vault.)
clients/
*_seed.sql
