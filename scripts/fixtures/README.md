# Test fixtures

- `voice-note-pt.wav` — a synthetic (macOS `say -v Luciana`, mu-law 8 kHz) Brazilian
  Portuguese recording of "reunião com a JM amanhã às duas da tarde". Used to verify
  the deployed POST /api/sessions/voice transcription (Deepgram Nova-3 on Workers AI)
  from a real admin browser session. No client data.
